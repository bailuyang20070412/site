<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<title>黄色超跑 · 终极完美版</title>
<style>
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  width: 100vw;
  height: 100vh;
  background: linear-gradient(to bottom, #101118, #050507);
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
}

.road {
  position: relative;
  width: 100%;
  height: 140px;
  background: #1a1b24;
  transform: skewX(-8deg);
  overflow: hidden;
  box-shadow: 0 0 50px rgba(0,0,0,0.9);
}

.road-line {
  position: absolute;
  bottom: 22px;
  width: 200%;
  height: 4px;
  background: repeating-linear-gradient(90deg, #fff 0 50px, transparent 50px 100px);
  animation: roadMove 0.5s linear infinite;
}

/* 车整体 */
.car {
  position: absolute;
  left: -450px;
  bottom: 0;
  width: 420px;
  height: 150px;
  animation: drive 6s linear infinite;
  transform-origin: center bottom;
}

/* 车身底部 */
.body-bottom {
  position: absolute;
  bottom: 38px;
  left: 26px;
  width: 360px;
  height: 52px;
  background: #ffcc00;
  border-radius: 10px;
  transform: skewX(-18deg);
  box-shadow: inset -6px -6px 15px rgba(0,0,0,0.25);
}

/* 车身顶部 */
.body-top {
  position: absolute;
  bottom: 68px;
  left: 98px;
  width: 200px;
  height: 36px;
  background: #ffdd33;
  border-radius: 14px 28px 0 0;
  transform: skewX(-24deg);
}

/* 车头 */
.head {
  position: absolute;
  bottom: 38px;
  left: 28px;
  width: 50px;
  height: 46px;
  background: #ffbb00;
  border-radius: 8px 0 0 6px;
  transform: skewX(-38deg);
}

/* 车尾 */
.tail {
  position: absolute;
  bottom: 38px;
  right: 38px;
  width: 48px;
  height: 46px;
  background: #e6a700;
  border-radius: 0 8px 6px 0;
  transform: skewX(-38deg);
}

/* 车窗 */
.window {
  position: absolute;
  bottom: 70px;
  left: 110px;
  width: 76px;
  height: 26px;
  background: #111;
  border-radius: 6px 14px 0 0;
  transform: skewX(-28deg);
}

/* 前灯 */
.light-front {
  position: absolute;
  bottom: 50px;
  left: 32px;
  width: 10px;
  height: 16px;
  background: #fff;
  border-radius: 2px;
  transform: skewX(-38deg);
  box-shadow: 0 0 12px #fff;
}

/* 尾灯 */
.light-back {
  position: absolute;
  bottom: 50px;
  right: 44px;
  width: 10px;
  height: 16px;
  background: #ff3333;
  border-radius: 2px;
  transform: skewX(-38deg);
  box-shadow: 0 0 12px #ff3333;
}

/* 车轮 */
.wheel {
  position: absolute;
  width: 54px;
  height: 54px;
  background: #111;
  border-radius: 50%;
  border: 12px solid #333;
  bottom: 10px;
  animation: wheelRotate 0.45s linear infinite;
  transform-origin: center center;
  box-shadow: 0 0 15px rgba(0,0,0,0.7);
  z-index: 2;
}

.wheel.front {
  left: 88px;
}

.wheel.rear {
  right: 66px;
}

/* 车轮中心装饰 */
.wheel::after {
  content: "";
  position: absolute;
  width: 10px;
  height: 10px;
  background: #666;
  border-radius: 50%;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
}

/* 地面阴影 */
.shadow {
  position: absolute;
  bottom: 6px;
  left: 50%;
  transform: translateX(-50%) skewX(20deg);
  width: 340px;
  height: 20px;
  background: rgba(0,0,0,0.4);
  border-radius: 50%;
  filter: blur(15px);
  z-index: -1;
}

/* 动画 */
@keyframes roadMove {
  0% { transform: translateX(0); }
  100% { transform: translateX(-100px); }
}

@keyframes drive {
  0% { transform: translateX(0) scale(0.9); }
  50% { transform: translateX(50vw) scale(1); }
  100% { transform: translateX(calc(100vw + 470px)) scale(0.9); }
}

@keyframes wheelRotate {
  0% { transform: rotate(0deg); }
  100% { transform: rotate(360deg); }
}

</style>
</head>

<body>

<div class="road">
  <div class="road-line"></div>

  <div class="car">
    <div class="shadow"></div>
    <div class="body-bottom"></div>
    <div class="body-top"></div>
    <div class="head"></div>
    <div class="tail"></div>
    <div class="window"></div>
    <div class="light-front"></div>
    <div class="light-back"></div>
    <div class="wheel front"></div>
    <div class="wheel rear"></div>
  </div>
</div>

</body>
</html>
